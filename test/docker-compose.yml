version: "3.7"

services:
  snowbound:
    image: snowbound:5.6.2-ark1
    build:
      context: ..
    init: true
    environment:
      CATALINA_OPTS: -Dsnowbound_host_port=http://localhost:8080 -Darkcase_host_port=http://arkcase:8080
    ports:
      - 8080:8080
